// Setup
import React from "react";

// Our logout component
const Logout = () => {
    return (
        <a className="mainLink" href="/logout">
            Logout
        </a>
    );
};

export default Logout;
